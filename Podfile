# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'synema' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for synema
  pod 'RxSwift'
  pod 'Moya/RxSwift'
  pod 'AlamofireImage'
  pod 'ObjectMapper'
  pod 'SVProgressHUD'

  target 'synemaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'synemaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
